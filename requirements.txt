# NaijaReview Intelligence — Python dependencies
# Kept in sync with pyproject.toml.
# Prefer `poetry install` for local dev. This file targets Docker / CI environments.
#
# Groups:
#   Base     — always required (API + LLM + auth)
#   ML       — required for full agent runs (ChromaDB, FAISS, embeddings)
#   Eval     — required for evaluation harness
#   Dev      — local development only

# ── Base ────────────────────────────────────────────────────────────────────
langchain-core>=0.3.0
langgraph>=0.3.0
google-genai>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
Jinja2>=3.1.4
structlog>=24.4.0
pyyaml>=6.0.2
httpx>=0.28.0

# ── Auth / Security ──────────────────────────────────────────────────────────
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# ── Database ─────────────────────────────────────────────────────────────────
sqlmodel>=0.0.21

# ── ML ───────────────────────────────────────────────────────────────────────
langchain-google-genai>=2.0.0
langchain-community>=0.3.0
chromadb>=0.5.0
faiss-cpu>=1.8.0
sentence-transformers>=3.3.0
FlagEmbedding>=1.3.0
rank-bm25>=0.2.2
spacy>=3.8.0
vaderSentiment>=3.3.2
redis>=5.2.0

# ── Eval ─────────────────────────────────────────────────────────────────────
rouge-score>=0.1.2
bert-score>=0.3.13
scikit-learn>=1.5.0
pandas>=2.2.0
matplotlib>=3.9.0
seaborn>=0.13.0

# ── Dev ──────────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-cov>=5.0.0
pytest-asyncio>=0.24.0
ruff>=0.8.0
mypy>=1.13.0
pre-commit>=4.0.0
ipykernel>=6.29.0
